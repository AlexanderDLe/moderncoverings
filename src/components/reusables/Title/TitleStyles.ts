import { makeStyles } from '@material-ui/core/styles';

export const TitleStyles = makeStyles((theme) => ({
    title: {
        paddingBottom: 0,
        marginBottom: 0,
        fontFamily: 'Raleway',
    },
}));
