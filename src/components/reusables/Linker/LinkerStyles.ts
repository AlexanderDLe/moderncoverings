import { makeStyles } from '@material-ui/core/styles';

export const LinkerStyles = makeStyles((theme) => ({
    link: {
        textDecoration: 'none',
        color: '#3f51b5',
    },
    buttonLink: {
        textDecoration: 'none',
        color: 'white',
    },
}));
